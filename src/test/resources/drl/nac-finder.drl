package drl;

import nl.jqno.foobal.domain.*;

dialect "mvel"

rule "Outcome where home team is NAC"
when
	$result  : ScoreKeeper()
	$outcome : Outcome( homeTeam == "NAC" )
then
	$result.add($outcome.homeScore, $outcome.outScore);
end